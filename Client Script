Step 1: Global UI Script (Updated Logic)
Name: CriticalRiskCalculator
Global: true
Active: true
javascriptfunction updateCriticalApplicationRisk() {
    var sox = g_form.getValue('u_sox');
    var glba = g_form.getValue('u_glba');
    var sensitive = g_form.getValue('u_sensitive');
    var sensitiveCommercial = g_form.getValue('u_sensitive_commerical');
    
    var soxYes = (sox == 'Yes');
    var glbaYes = (glba != '0' && glba != '' && glba != null);
    var sensitiveYes = (sensitive != '0' && sensitive != '' && sensitive != null);
    var sensitiveCommercialYes = (sensitiveCommercial != '0' && sensitiveCommercial != '' && sensitiveCommercial != null);
    
    var anyFieldYes = soxYes || glbaYes || sensitiveYes || sensitiveCommercialYes;
    var currentValue = g_form.getValue('u_critical_application_risk');
    
    if (!anyFieldYes) {
        // All fields are No, set critical app to No
        if (currentValue != 'No') {
            g_form.setValue('u_critical_application_risk', 'No');
        }
    } else if (currentValue == 'No') {
        // Currently No and at least one field is Yes, set to Yes
        g_form.setValue('u_critical_application_risk', 'Yes');
    }
    // Otherwise leave it alone (already Yes or YesRBAC)
}

Step 2: Create 4 Client Scripts
Client Script 1
Name: Update Critical Risk - SOX
Table: [Your table]
Type: onChange
Field name: u_sox
javascriptfunction onChange(control, oldValue, newValue, isLoading, isTemplate) {
    if (isLoading) return;
    updateCriticalApplicationRisk();
}
Client Script 2
Name: Update Critical Risk - GLBA
Table: [Your table]
Type: onChange
Field name: u_glba
javascriptfunction onChange(control, oldValue, newValue, isLoading, isTemplate) {
    if (isLoading) return;
    updateCriticalApplicationRisk();
}
Client Script 3
Name: Update Critical Risk - Sensitive
Table: [Your table]
Type: onChange
Field name: u_sensitive
javascriptfunction onChange(control, oldValue, newValue, isLoading, isTemplate) {
    if (isLoading) return;
    updateCriticalApplicationRisk();
}
Client Script 4
Name: Update Critical Risk - Sensitive Commercial
Table: [Your table]
Type: onChange
Field name: u_sensitive_commerical
javascriptfunction onChange(control, oldValue, newValue, isLoading, isTemplate) {
    if (isLoading) return;
    updateCriticalApplicationRisk();
}
